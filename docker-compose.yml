services:
  backend:
    build: ./backend
    volumes:
      - sqlite_data:/app/db
    expose:
      - "9292"

  frontend:
    build: ./frontend
    ports:
      - "4200:4200"
    depends_on:
      - backend

volumes:
  sqlite_data:
